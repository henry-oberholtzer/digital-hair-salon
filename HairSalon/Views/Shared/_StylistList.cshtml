@model List<Stylist>

<ul>
@if (Model.Count == 0)
{
  <li>No clients have been added.</li>
}

@foreach (Stylist stylist in Model)
{
  <li><a asp-controller="Stylists" asp-action="Details" asp-route-id=@stylist.StylistId>@stylist.Name</a></li>
}
</ul>
