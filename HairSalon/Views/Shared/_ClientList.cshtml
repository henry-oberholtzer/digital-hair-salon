@model List<Client>

<ul>
@if (Model.Count == 0)
{
  <li>No clients have been added.</li>
}

@foreach (Client client in Model)
{
  <li><a asp-controller="Clients" asp-action="Details" asp-route-id=@client.ClientId>@client.Name</a></li>
}
</ul>
